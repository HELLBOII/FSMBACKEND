{
  "name": "fms-backend",
  "version": "1.0.0",
  "description": "Backend API server for FMS (Field Management System) - Email service",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "express",
    "nodemailer",
    "api",
    "email-service"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.1",
    "nodemailer": "^8.0.1",
    "cors": "^2.8.6",
    "dotenv": "^17.2.4"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}

